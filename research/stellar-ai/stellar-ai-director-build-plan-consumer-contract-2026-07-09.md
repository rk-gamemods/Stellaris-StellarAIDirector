# Stellar AI Director Build-Plan Consumer Contract

Date: 2026-07-09

Primary development target: Stellaris PC 4.4.5. Required compatibility/runtime
baseline: pinned Pegasus v4.4.4 (5505).

Scope: static source-backed contract for consuming the generated Stellar AI
Director economic/building model. This note does not prove runtime behavior and
does not authorize observer-run validation.

## Source Evidence

Local vanilla source was indexed as `local/stellaris_445_common_build_plan_surfaces`
from `C:\Steam\steamapps\common\Stellaris\common`, covering:

- `economic_plans/`
- `colony_automation/`
- `buildings/`
- `districts/`
- `scripted_triggers/`
- `scripted_effects/`

JDocMunch indexed 142 `.txt` files and 11671 sections with embeddings disabled.
A 200-section `verify_index` sample had 0 drift, 0 missing, and 0 errors.
JCodeMunch was attempted first for the same vanilla folders but returned
`No source files found`, so the `.txt` PDXScript surfaces were inspected through
JDocMunch instead.

Relevant vanilla evidence:

- `economic_plans/00_example.txt` says economic plans are additive by entry:
  duplicate plan names are merged, duplicate subplan names overwrite, and
  non-subplan fields usually overwrite depending on field type.
- `economic_plans/00_example.txt` defines `income`, `focus`, `subplan`,
  `scaling_subplan`, `optional_subplan`, `pops`, `empire_size`, `naval_cap`,
  and country-scope `ai_weight` as economic-plan fields.
- `buildings/00_example.txt` defines `potential` as the construction-list
  display trigger and `allow` as the trigger that allows or grays out
  construction.
- `buildings/00_example.txt` defines `abort_trigger` and
  `abort_construction_trigger` as ongoing-construction cancellation triggers.
- `buildings/00_example.txt` defines `destroy_trigger` as the trigger that
  destroys or more likely converts a building, and `convert_to` as conversion
  candidates when that trigger fires.
- `buildings/00_example.txt` states building `ai_weight` is not used anymore
  unless economic plans are overwritten not to be used.
- `buildings/11_primitive_buildings.txt` includes concrete building definitions
  with `can_build = no`, `can_demolish = yes`, `can_be_ruined = no`,
  `planet_limit`, and `category`.
- `colony_automation/00_research_automation.txt` shows designation automation
  entries with `category`, `available`, `prio_districts`, `prio_zones`, and an
  ordered `buildings` list.

Current Director evidence:

- `mods/StellarAIDirector/common/economic_plans/zzzz_staid_additive_economic_plan.txt`
  is generated by `tools/generate_stellar_ai_director_patch.py` and intentionally
  replaces `basic_economy_plan` for the active high-scale mod stack.
- The current Director economic plan sets high research/alloy/survival
  `income` and `focus` targets and emits many optional/scaling `subplan` routes.
- The Director has no `common/colony_automation` files at this checkpoint.
- Generated model data validated through JDataMunch includes 826 readiness rows,
  592 current build-plan candidates, and 789 gated rows with same-role fallback
  candidates.
- Generated blocker accounting has 396 rows after the corrective manual source
  audit. Unknown-job blockers are zero; remaining blockers are 265
  benefit-formula rows and 131 unresolved-variable rows.

## Consumer Contract

1. Economic plans are the primary consumer surface for strategic resource
   pressure.
   - Use generated resource scenarios, role targets, and source-backed benefit
     classes to derive `income`, `focus`, and named optional/scaling `subplan`
     entries.
   - Prefer additive subplans with stable names when the intent is to add
     pressure without replacing unrelated base-plan fields.
   - Treat full `basic_economy_plan` replacement as high-risk and intentional;
     the current Director already does this as a mod-stack-specific override.

2. Colony automation is not a proven AI-empire construction consumer.
   - Current evidence identifies `common/colony_automation` as Planetary and
     Sector Automation with designation-specific `available`, `prio_districts`,
     `prio_zones`, and ordered `buildings` lists.
   - No inspected source proves AI-controlled empires consume those lists as a
     strict build-plan whitelist. Do not generate Director colony automation as
     an AI strategy fix without a current vanilla call site or runtime trace.
   - The syntax remains useful evidence for player automation ordering only.

3. Building `ai_weight` is not a valid primary consumer surface for this model
   while economic plans are active.
   - Do not emit per-building `ai_weight` as the main strategy channel.
   - Only revisit building `ai_weight` if a future source-backed design disables
   or replaces economic-plan usage for the relevant behavior.

4. Construction scoring fields are distinct from building `ai_weight`.
   - `ai_resource_production` maps an object's output into economic-plan demand.
   - `ai_weight_coefficient` and `additional_ai_weight` participate in
     construction scoring and must preserve source intent unless a narrow,
     source-backed override owns them.
   - Global construction defines such as deficit, focus, resource-production,
     naval-cap, opportunity-cost, and build-threshold multipliers shape the
     computed score. They are secondary policy inputs, not a strict colony
     whitelist.

5. Building availability gates are hard filters, not scoring hints.
   - `potential` controls whether the building is displayed in the construction
     list.
   - `allow` controls whether construction is permitted or grayed out.
   - `prerequisites`, event/unlock flags, capital-tier hints, `can_build`, and
     generated readiness phase must be checked before a building is emitted as a
     construction candidate.
   - `can_build = no` objects are not build-plan candidates even when their
     benefits are useful to the accounting model.

6. Destroy, conversion, and demolition are not discretionary replacement proof.
   - `destroy_trigger` and `convert_to` are source-backed game-driven state
     transitions when their triggers fire.
   - `can_demolish` describes whether a building is demolishable, but the
     inspected colony-automation and economic-plan sources do not show a static
     build-plan command that proactively demolishes or refactors existing
     buildings.
   - Therefore generated fallback buildings must be treated as permanent or
     no-regret unless the consumer adds a separate source-backed replacement
     mechanism or runtime proof.

7. Fallback policy is conservative by default.
   - A fallback may be emitted when it is base/scripted available, same-role,
     terminal or intentionally nonterminal, and not blocked by unresolved
     accounting rows.
   - A fallback must not be emitted solely because a later gated building has
     better modeled output.
   - If a later generator wants temporary fallbacks, it must add an explicit
     consumer policy column such as `fallback_lifetime` with values like
     `permanent`, `until_upgrade_chain`, `until_destroy_trigger_conversion`, or
     `runtime_required`. Until that column exists, treat fallback rows as
     conservative suggestions only.

8. Benefit taxonomy rows are not all directly scorable.
   - `numeric_preserved` rows may feed formulas only after their class-specific
     consumer rule is defined.
   - `detected_unvalued` and `not_observed` rows must remain detected-only
     blockers or policy rows; do not turn them into arbitrary weights.
   - Resource-like benefits should flow first into economic-plan resource
     targets. Non-resource benefits need explicit class rules before they affect
     construction ordering.

9. Blocker accounting is a stop sign for final consumption.
   - Unresolved variables and unvalued benefit formulas must either be resolved,
     explicitly excluded, or policy-classified before their objects become final
     generated build-plan facts.
   - Source-proven zero-effect job references belong in the
     `source_excluded_jobs` audit field, not in final scoring.
   - The blocker CSV is allowed to remain nonempty during modeling, but a build
     plan generator must filter or annotate affected rows rather than scoring
     them as complete.

## Required Generator Rules

Before a generated build-plan implementation consumes the model, enforce these
rules deterministically:

- Reject or annotate any emitted construction candidate whose readiness row has
  `build_plan_candidate != yes`.
- Reject or annotate any emitted construction candidate tied to unresolved
  blocker-accounting rows unless the row's issue type has an explicit consumer
  policy.
- Do not emit building `ai_weight` as the main strategy surface.
- Do not mark fallback rows as replaceable unless a source-backed replacement
  path is attached.
- Keep economic-plan subplan names stable so additive merges and targeted
  overwrites are predictable.
- Do not emit AI-strategy `common/colony_automation` files until an AI-empire
  consumer is source-proven. If player automation support is intentionally
  added later, validate every referenced ID and label that scope explicitly.
- Preserve parent `ai_weight_coefficient` and `additional_ai_weight` unless a
  narrow override documents the active construction-score contract.
- Require military-capacity objects to use explicit hard eligibility when they
  can consume specialized economic-world slots.

## Milestone Status

- M12 answer: static evidence supports construction ordering and game-triggered
  destroy/convert transitions, but not proactive automation-driven demolition or
  refactoring. Treat proactive replacement as unsupported until implemented with
  a separate source-backed mechanism or runtime proof.
- M13 answer: fallback buildings are source-backed candidates, but their
  replacement lifetime is conservative. Do not assume temporary fallback
  replacement from vanilla colony automation syntax alone.
- M15 corrected answer: economic plans consume strategic resource pressure;
  `ai_resource_production`, coefficients/additive score fields, designation
  affinity, and global construction defines shape candidate scores; hard
  `potential`/`allow` gates define eligibility. Building `ai_weight` is inactive
  with economic plans, and no strict AI-empire colony-automation whitelist has
  been proven.

## Open Work

- Keep the generated consumer-policy artifact as the source of truth for
  readiness, blocked rows, role targets, benefit taxonomy, and deterministic
  non-candidate exclusions.
- Keep tests for `build_plan_candidate`, no `ai_weight`-only strategy emission,
  fallback lifetime classification, source-excluded job audit rows, and blocked
  unresolved consumer statuses.
- Do not tune build priorities from rows tied to the 396 remaining blockers.
  Finish variable resolution and benefit-formula policy first.
- Reconstruct the active construction-score contract from current vanilla
  defines and object fields, then map each generated dataset column to a proven
  consumer or mark it evidence-only.
- Prove or reject any AI-empire use of `common/colony_automation` through a
  current vanilla reference or an explicitly approved runtime trace.
